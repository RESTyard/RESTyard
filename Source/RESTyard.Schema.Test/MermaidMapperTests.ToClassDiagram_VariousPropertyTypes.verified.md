```mermaid
classDiagram
    class Product {
        +integer id
        +string name
        +number price
        +boolean isActive
        +array tags
        +object address
        +null nullableField
        +object noTypeField
        +object nestedObject
        +object refField
    }
```