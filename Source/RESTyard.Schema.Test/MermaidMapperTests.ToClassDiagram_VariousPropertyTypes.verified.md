```mermaid
classDiagram
    class Product {
        +integer id
        +string name
        +number price
        +boolean isActive
        +string[] tags
        +object address
        +null nullableField
        +object noTypeField
        +object nestedObject
        +Brand refField
    }
```